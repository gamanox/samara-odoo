<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <template id="product_intention_form_inherit" inherit_id="website_sale.product">
    <xpath expr="//div[@id='product_details'][h1[@itemprop='name' and normalize-space()='Intenciones de Misa']]" position="after">
      <!-- Show the custom intention form only when the product has the INTENTION tag -->
     
     
        <form action="/shop/intention/add" method="post" class="o_intention_form o_we_website_form mt-3">
          <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
          <input type="hidden" name="product_id" t-att-value="product.product_variant_id.id"/>
          <div class="row g-3">
            <div class="col-md-6">
              <label>Asunto / Nombre</label>
              <input class="form-control" type="text" name="name" required="required"/>
            </div>
            <div class="col-md-6">
              <label>Fecha y hora</label>
              <input class="form-control" type="datetime-local" name="date_time" required="required"/>
            </div>
            <div class="col-md-6">
              <label>Lugar</label>
              <select class="form-select" name="church" required="required">
                <option value="main">Templo principal</option>
                <option value="chapel">Capilla</option>
              </select>
            </div>
            <div class="col-md-6">
              <label>Ofrenda</label>
              <input class="form-control" type="number" step="0.01" min="0" name="amount"/>
            </div>
            <div class="col-12">
              <label>Intención / Mensaje</label>
              <textarea class="form-control" name="message" rows="3"></textarea>
            </div>
            <div class="col-12">
              <button class="btn btn-primary mt-2" type="submit">Añadir intención al carrito</button>
            </div>
          </div>
        </form>
      
    </xpath>
  </template>
</odoo>
